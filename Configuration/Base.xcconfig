// Default open-source configuration
DEVELOPMENT_TEAM =
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY = Apple Development

APP_BUNDLE_ID = com.example.BambuGateway
SHARE_EXTENSION_BUNDLE_ID = $(APP_BUNDLE_ID).share-extension
LIVE_ACTIVITY_BUNDLE_ID = $(APP_BUNDLE_ID).LiveActivity

#include? "LocalSigning.xcconfig"
